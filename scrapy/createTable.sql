CREATE TABLE position (
  id                INT PRIMARY KEY NOT NULL,
  orderBy           INT,
  leaderName        VARCHAR(30),
  calcScore         VARCHAR(10),
  companySize       VARCHAR(30),
  deliverCount      INT,
  countAdjusted     VARCHAR(10),
  flowScore         INT,
  workYear          VARCHAR(20),
  education         VARCHAR(15),
  financeStage      VARCHAR(30),
  pvScore           VARCHAR(30),
  city              VARCHAR(10),
  companyLogo       VARCHAR(50),
  createTimeSort    VARCHAR(40),
  companyId         VARCHAR(20),
  industryField     VARCHAR(30),
  createTime        VARCHAR(20),
  score             INT,
  imstate           VARCHAR(20),
  adWord            INT,
  formatCreateTime  VARCHAR(20),
  showOrder         INT,
  salary            VARCHAR(20),
  positionName      VARCHAR(20),
  companyName       VARCHAR(20),
  jobNature         VARCHAR(20),
  positionTypesMap  VARCHAR(10),
  totalCount        INT,
  positionFirstType VARCHAR(10),
  relScore          INT,
  companyLabelList  VARCHAR(20),
  positionId        VARCHAR(30),
  randomScore       INT,
  companyShortName  VARCHAR(40),
  showCount         INT,
  haveDeliver       VARCHAR(10),
  hrScore           INT,
  plus              VARCHAR(20),
  positionType      VARCHAR(20),
  searchScore       VARCHAR(10),
  positionAdvantage VARCHAR(20),
  adjustScore       INT
);